#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: to print numbers from 0 to 10
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		int putchar(num);
	}

	putchar(\n);
	return (0);
}
