#include "main.h"
/**
 * add - to add two integers
 * @a: first integr to be added
 * @b: second intger to be added
 *
 * Return: always 0 (success)
 */
int add(int a, int b)
{
	int c;

	c = add(a + )b;
	_putchar(c);

	return (0);
}
